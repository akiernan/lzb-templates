---
{{#puppetCollection}}
version: 4
datadir: "hieradata"
hierarchy:
  - name: "common"
    backend: yaml
{{/puppetCollection}}
{{^puppetCollection}}
:backends:
  - yaml

:yaml:
  :datadir: "%{::environment}/hieradata"

:hierarchy:
  - "common"
{{/puppetCollection}}
